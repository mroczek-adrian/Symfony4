{% extends 'base.html.twig' %}

{% block title %}Hello Articl{% endblock %}



{% block body %}

    <div class="form-group row">
        <div class="col-sm-1">

        </div>
        <div class="col-sm-1">
            <li><a href="{{path('hello_page') }} ">back</a></li>
        </div>
        <div class="col-sm-8">


            <h1>Zajecia! </h1><br>
            {% for zaj in allZajecia %}


                <li>Id {{ zaj.id }} Nazwa zajec: {{ zaj.nazwa }} Czas trwania {{ zaj.okres }} h <a href="{{ url('usuniecia_artykulu', { 'id': zaj.id }) }}">Usun</a></li>



            {% endfor %}
            <br><h1>Launch a new Zajecia! </h1>
            {{ form_start(zajeciaForm) }}
            {{ form_widget(zajeciaForm) }}
            <button type="submit" class="btn btn-primary">Create!</button>
            {{ form_end(zajeciaForm) }}


        </div>
        <div class="col-sm-2"></div>
    </div>




{#    <h1>Launch a new Article! ?</h1>#}
{#    {{ form_start(articleForm) }}#}
{#    {{ form_widget(articleForm) }}#}
{#    <button type="submit" class="btn btn-primary">Create!</button>#}
{#    {{ form_end(articleForm) }}#}
    <div class="form-group row">
        <div class="col-sm-1">

        </div>
        <div class="col-sm-1">

        </div>
        <div class="col-sm-8">
            <h1>Godziny pracy uczelni! </h1><br>
            {% for godz in allDniUczelni %}


                <li>Id {{ godz.id }} Dzien {{ godz.dzien }} Godziny {{ godz.godziny }}  <a href="{{ url('usuniecia_godz_ucz', { 'id': godz.id }) }}">Usun</a></li>



            {% endfor %}
            <br> <h1>Launch a new Godziny Pracy Uczelni </h1>
            {{ form_start(gpuForm) }}
            {{ form_widget(gpuForm) }}
            <button type="submit" class="btn btn-primary">Create!</button>
            {{ form_end(gpuForm) }}



        </div>
        <div class="col-sm-2">

        </div>
    </div>



    <div class="form-group row">
        <div class="col-sm-1">

        </div>
        <div class="col-sm-1">

        </div>
        <div class="col-sm-8">
            <h1>Nauczyciele </h1><br>
            {% dump(allTeachers) %}
            {% for teach in allTeachers %}
                <li>Id {{ teach.id }}Name {{ teach.name }} Surname {{ teach.surname}} Subject {{ teach.subject }} Dni wolne od pracy: {{ teach.day1 }},{{ teach.day3 }},{{ teach.day2 }}<a href="{{ url('usuniecia_teachers', { 'id': teach.id }) }}">Usun</a></li>
            {% endfor %}
            <br> <h1>Dodaj nauczyciela </h1>
            {{ form_start(teachersForm) }}
            {{ form_widget(teachersForm) }}
            <button type="submit" class="btn btn-primary">Create!</button>
            {{ form_end(teachersForm) }}
        </div>
        <div class="col-sm-2">

        </div>
    </div>





{% endblock %}

